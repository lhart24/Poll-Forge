# web-tool
web tool

use ms library for time 
docker for later
react for front end
node for back end
have to host front end and back end seperatly (vercel and railway)
find what data to store e.g. store api endpoint history but not the key itself in database (find out later)